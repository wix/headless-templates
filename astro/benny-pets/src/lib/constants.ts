/**
 * Wix Stores' app id — the same value on every site, used to build the
 * catalog reference a cart line item needs.
 */
export const STORES_APP_ID = "1380b703-ce81-ff05-f115-39571d94dfcd";

/**
 * The contact form rendered in the footer.
 *
 * This GUID comes from the site template this code is paired with in
 * `templates.json`; a site created from that template provisions the same
 * form. Point it at your own form id (Wix dashboard → Forms) if you connect
 * this code to a site you built yourself.
 */
export const CONTACT_FORM_ID = "db5a5c36-3337-4163-be77-e41e09f7216e";
