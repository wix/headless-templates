/**
 * The ID of the registration form that is provisioned together with this
 * template's Wix site, so it works out of the box for sites created from the
 * template. If you connect this project to a different Wix site, replace it
 * with your own form's ID: open your site's dashboard, go to Forms, open the
 * form, and copy the form ID from the page URL (or fetch your forms
 * programmatically with the `@wix/forms` SDK).
 */
export const WIX_FORM_ID = "034534ec-a653-4d79-9f1d-e484f0d35321";

/**
 * The business name displayed in the header, footer, and page copy.
 * Replace it with your own, or source it dynamically from your Wix site's
 * properties (Site Properties API).
 */
export const BUSINESS_NAME = "Business Name";
